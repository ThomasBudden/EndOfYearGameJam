using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KidnapManager : MonoBehaviour
{
    public float f; //f = Influence / dark energy - currency allows you to unlock and upgrade cards, allows you to get win condition after accumulating a certain amount.
    public float c; //c =Cultists - people amount of people in cult 
    public float o; //o = loyalty - how loyal they are to cult
    public float i; //i = infamy - negative multiplier that affects influence
    public float a; //a = initial number
    public float t; //t = time interval
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
